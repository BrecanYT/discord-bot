export declare class GoogleImageSearch {
    private readonly apiKey;
    private readonly searchEngineId;
    constructor(apiKey: string, searchEngineId: string);
    getImageUrls(searchString: string): Promise<any>;
    getImageUrl(searchString: string): Promise<any>;
}
